using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using libFSTest;
//FSライブラリが動くかテストしたやつ
public class dlltest : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        /*
        var p = new Vector3(1, 3, 5);
        var e = new test.elm(1, 3, 4);
        var lis = new test.elm[100];
        for (int i = 0; i < 100; i++)
        {
            lis[i] = new test.elm(i, i * 4, 1);
        }
        var sl = new test.sortedList(lis);

        Debug.Log(sl.Sarch(20));
        */
    }

    // Update is called once per frame
    void Update()
    {

    }
}
